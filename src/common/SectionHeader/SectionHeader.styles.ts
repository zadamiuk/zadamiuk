import styled from 'styled-components';
import { COLORS } from '../../constants/colors';

export const Header = styled.p`
  font-size: 20px;
  font-weight: bold;
  color: ${COLORS.header};
  padding: 0;
  margin: 10px 0;
`;
